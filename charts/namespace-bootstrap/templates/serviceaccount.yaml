{{- range .Values.namespaces }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .service }}-sa
  namespace: {{ .name }}
  labels:
    environment: {{ .env }}
    service: {{ .service }}
    managed-by: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
{{- end }}
