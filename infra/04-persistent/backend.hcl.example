# Copy to backend.hcl (gitignored). Then: terraform init -backend-config=backend.hcl
bucket       = "aria-tfstate-<ACCOUNT_ID>"
key          = "persistent/terraform.tfstate"
region       = "ap-southeast-1"
use_lockfile = true
encrypt      = true
