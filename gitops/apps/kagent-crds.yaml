# Wave 0 — kagent CRDs (upstream OCI Helm chart; NOT vendored).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kagent-crds
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: ghcr.io/kagent-dev/kagent/helm
    chart: kagent-crds
    targetRevision: "0.10.0-rc1" # bumped 2026-08-16: unblocks SandboxAgent (substrate + agent-sandbox); see docs/execution-environment.md
  destination:
    server: https://kubernetes.default.svc
    namespace: kagent
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false # kagent ns is created by Terraform (infra/03-argocd)
      - ServerSideApply=true # CRDs are large — avoid the client-side annotation limit
